RITICAL PRIORITY ORDER - Execute in this exact sequence:
PHASE 1: CORE WEB VITALS FIXES (Complete First)
Target Metrics:

LCP (Largest Contentful Paint): Under 2.0 seconds
INP (Interaction to Next Paint): Under 150ms
CLS (Cumulative Layout Shift): Under 0.05

1. LCP Optimization (Loading Speed):

Compress all images to WebP format, max 800KB per image
Remove all unused JavaScript and CSS files
Minify remaining CSS/JS files
Implement lazy loading for images below fold only
Preload critical hero section images
Remove any third-party scripts not essential for form functionality
Optimize web fonts: use font-display: swap

2. INP Optimization (Responsiveness):

Minimize JavaScript execution time
Remove all unused plugins and libraries
Optimize form interactions and validation
Use content-visibility: auto for below-fold content
Defer non-critical JavaScript loading

3. CLS Optimization (Layout Stability):

Set fixed width/height attributes for all images
Reserve space for dynamic content (forms, modals)
Avoid inserting content above existing content
Ensure consistent button and form element sizing

Performance Testing Required:

Test with Google PageSpeed Insights after each change
Must achieve 90+ mobile score before proceeding to Phase 2
Verify improvements in Google Search Console Core Web Vitals report

PHASE 2: UI/UX MODERNIZATION (Only After CWV Fixed)
Design System:
Colors:

Primary: #3b82f6 (modern blue)
Secondary: #10b981 (success green)
Text: #1f2937 (dark gray)
Background: #ffffff (white)
Light backgrounds: #f8fafc, #e2e8f0

Typography:

Font: system-ui, -apple-system, 'Segoe UI', sans-serif (no custom fonts)
Headline: 48px, font-weight 700, line-height 1.2
Subheading: 24px, font-weight 600, line-height 1.4
Body: 16px, font-weight 400, line-height 1.6
Small text: 14px, font-weight 500

Spacing System:

Section padding: 80px vertical, 24px horizontal
Card padding: 32px
Button padding: 16px vertical, 32px horizontal
Form field spacing: 16px between fields
Grid gaps: 24px

Component Updates:
Buttons:

Border-radius: 8px
Box-shadow: 0 2px 8px rgba(59, 130, 246, 0.15)
Transition: all 0.2s ease
Hover: transform: translateY(-1px)

Form Elements:

Border-radius: 6px
Border: 1px solid #e2e8f0
Focus: border-color #3b82f6, box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1)
Height: 48px for inputs

Cards/Sections:

Border-radius: 12px
Box-shadow: 0 4px 12px rgba(0, 0, 0, 0.05)
Background: #ffffff

Hero Section Specific:

Maintain current layout and form functionality
Update only colors, spacing, and typography
Ensure modal form maintains performance optimizations

Performance Stats Section:

Update 99.2%, 97.8%, 98.5% cards with modern styling
Add subtle hover animations (transform: scale(1.02))
Improve visual hierarchy with better typography

Mobile Optimizations:

Minimum touch target: 44px
Responsive grid: single column on mobile
Optimized form layouts for mobile
Consistent mobile navigation

PHASE 3: TESTING & VALIDATION
Performance Verification:

Re-test Core Web Vitals after UI changes
Ensure no regression in performance scores
Monitor for 72 hours after deployment

Cross-browser Testing:

Chrome, Firefox, Safari, Edge
Mobile testing on iOS and Android
Form functionality verification

Quality Assurance:

All forms submit correctly
Email notifications working
Modal functionality intact
Navigation and links functional

CRITICAL CONSTRAINTS:
DO NOT Change:

Form field names or backend integration
URL structure or routing
Email notification system
Multi-step form workflow
Any JavaScript functionality

Performance Budget:

Maximum file size increases: 10%
No additional HTTP requests
No custom font loading
No heavy animations or transitions

Success Metrics:

Core Web Vitals: All green in PageSpeed Insights
Mobile performance score: 90+
Desktop performance score: 95+
No increase in bounce rate
Form conversion rate maintained or improved

Execute Phase 1 completely before starting Phase 2. Test performance after every change.