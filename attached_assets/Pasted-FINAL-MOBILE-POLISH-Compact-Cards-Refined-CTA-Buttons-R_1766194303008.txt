FINAL MOBILE POLISH - Compact Cards + Refined CTA Buttons:

REMAINING ISSUES TO FIX:

1. "Assistance Raccordement" section still has large vertical cards
2. "Tous les Types de Raccordement" section still has large vertical cards  
3. Hero CTA buttons are too bulky and not elegant

═══════════════════════════════════════════════════════════

FIX #1: COMPACT "ASSISTANCE RACCORDEMENT" CARDS (2x2 GRID)

Current problem: 4 large vertical cards stacked (Appelez-nous, Rappel gratuit, Chat, Formulaire)

SOLUTION - Convert to compact 2x2 grid:

```css
@media (max-width: 768px) {
  .assistance-container {
    max-width: 100%;
    padding: 40px 20px;
  }
  
  .assistance-title {
    font-size: 24px;
    font-weight: 700;
    color: #111827;
    margin-bottom: 8px;
  }
  
  .assistance-subtitle {
    font-size: 15px;
    color: #6B7280;
    margin-bottom: 24px;
  }
  
  /* 2x2 Grid Layout */
  .assistance-options {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
    margin-bottom: 40px;
  }
  
  /* Compact card */
  .assistance-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 20px 12px;
    min-height: 100px;
    background: #FFFFFF;
    border: 1px solid #E5E7EB;
    border-radius: 8px;
    text-decoration: none;
    transition: all 0.15s ease;
  }
  
  .assistance-card:active {
    transform: scale(0.98);
    border-color: #2563EB;
    background: #F9FAFB;
  }
  
  /* Smaller icon */
  .assistance-card-icon {
    width: 32px;
    height: 32px;
    margin-bottom: 8px;
    opacity: 0.9;
  }
  
  /* Compact text */
  .assistance-card-label {
    font-size: 14px;
    font-weight: 600;
    color: #2563EB;
    text-align: center;
    line-height: 1.3;
  }
}
RESULT: 4 cards in 2x2 grid, each ~100px height instead of ~140px

═══════════════════════════════════════════════════════════

FIX #2: COMPACT "TOUS LES TYPES" CARDS (HORIZONTAL LAYOUT)

Current problem: Large vertical cards with icon at top, title, description, and "Faire une demande" link

SOLUTION - Convert to horizontal compact cards:

Copy@media (max-width: 768px) {
  .types-section {
    padding: 40px 20px;
    background: #F9FAFB;
  }
  
  .types-title {
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 8px;
  }
  
  .types-description {
    font-size: 15px;
    color: #6B7280;
    margin-bottom: 24px;
  }
  
  /* Cards container */
  .types-cards {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  
  /* Horizontal compact card */
  .type-card {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 14px 16px;
    background: #FFFFFF;
    border: 1px solid #E5E7EB;
    border-radius: 8px;
    text-decoration: none;
    transition: all 0.15s ease;
  }
  
  .type-card:active {
    transform: scale(0.99);
    border-color: #2563EB;
  }
  
  /* Icon - left side, smaller */
  .type-card-icon {
    width: 44px;
    height: 44px;
    flex-shrink: 0;
  }
  
  /* Content - middle */
  .type-card-content {
    flex: 1;
    text-align: left;
  }
  
  .type-card-title {
    font-size: 16px;
    font-weight: 600;
    color: #111827;
    margin: 0 0 3px 0;
    line-height: 1.3;
  }
  
  .type-card-description {
    font-size: 13px;
    color: #6B7280;
    margin: 0;
    line-height: 1.4;
  }
  
  /* Hide "Faire une demande" text - card itself is clickable */
  .type-card-link {
    display: none;
  }
  
  /* Arrow - right side */
  .type-card-arrow {
    width: 18px;
    height: 18px;
    color: #9CA3AF;
    flex-shrink: 0;
  }
  
  /* Simple SVG arrow */
  .type-card-arrow svg {
    width: 100%;
    height: 100%;
  }
}
HTML STRUCTURE SHOULD BE:

Copy<a href="#" class="type-card">
  <img src="icon.svg" class="type-card-icon" alt="">
  <div class="type-card-content">
    <h3 class="type-card-title">Raccordement Provisoire</h3>
    <p class="type-card-description">Pour chantiers, événements temporaires et installations provisoires</p>
  </div>
  <svg class="type-card-arrow" viewBox="0 0 20 20" fill="currentColor">
    <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
  </svg>
</a>
RESULT: Each card reduced from ~200px height to ~72px height (64% reduction)

═══════════════════════════════════════════════════════════

FIX #3: REFINED HERO CTA BUTTONS (ELEGANT, NOT BULKY)

Current problem: Buttons look too thick/bulky, not elegant enough for professional B2B

SOLUTION - Sleeker, more refined button design:

Copy@media (max-width: 768px) {
  /* Button container */
  .hero-cta-buttons {
    display: flex;
    flex-direction: column;
    gap: 12px;
    padding: 0 20px;
    margin-top: 32px;
  }
  
  /* Primary CTA - Refined */
  .btn-primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    
    /* Refined sizing */
    padding: 15px 28px;
    min-height: 52px; /* Reduced from 56px */
    width: 100%;
    
    /* Typography */
    font-size: 16px; /* Reduced from 18px */
    font-weight: 600;
    letter-spacing: 0.01em;
    
    /* Colors */
    background: #2563EB;
    color: #FFFFFF;
    border: none;
    
    /* Shape - less rounded for elegance */
    border-radius: 6px; /* Reduced from 8px or 12px */
    
    /* Subtle shadow */
    box-shadow: 0 1px 3px rgba(37, 99, 235, 0.15);
    
    /* Smooth transition */
    transition: all 0.2s ease;
  }
  
  .btn-primary:active {
    transform: translateY(1px);
    box-shadow: 0 1px 2px rgba(37, 99, 235, 0.2);
  }
  
  /* Secondary CTA - Refined */
  .btn-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    
    /* Refined sizing */
    padding: 13px 24px;
    min-height: 48px; /* Reduced from 52px */
    width: 100%;
    
    /* Typography */
    font-size: 16px; /* Reduced from 18px */
    font-weight: 500; /* Reduced from 600 */
    
    /* Colors */
    background: transparent;
    color: #2563EB;
    border: 1.5px solid #2563EB; /* Thinner border */
    
    /* Shape */
    border-radius: 6px;
    
    /* Transition */
    transition: all 0.2s ease;
  }
  
  .btn-secondary:active {
    background: #EFF6FF;
    border-color: #1E40AF;
    color: #1E40AF;
  }
  
  /* Icon inside button (phone icon) */
  .btn-icon {
    width: 18px;
    height: 18px;
  }
}
DESKTOP BUTTONS (less bulky too):

Copy@media (min-width: 769px) {
  .hero-cta-buttons {
    display: flex;
    flex-direction: row;
    gap: 16px;
    justify-content: flex-start;
  }
  
  .btn-primary, .btn-secondary {
    width: auto;
    min-width: 180px;
    padding: 13px 32px;
    min-height: 48px;
    font-size: 16px;
  }
  
  .btn-secondary {
    font-weight: 500;
  }
}
KEY CHANGES FOR ELEGANCE:

Reduced height: 56px → 52px (primary), 52px → 48px (secondary)
Reduced font size: 18px → 16px
Less rounded corners: 8px/12px → 6px
Thinner secondary border: 2px → 1.5px
Secondary font-weight: 600 → 500
Subtler shadows
Tighter padding
═══════════════════════════════════════════════════════════

FIX #4: DESKTOP HERO BUTTONS (SIDE-BY-SIDE, ELEGANT)

On desktop, buttons should be side-by-side, not full-width:

Copy@media (min-width: 769px) {
  .hero-section {
    display: flex;
    align-items: center;
    padding: 80px 40px;
  }
  
  .hero-content {
    max-width: 600px;
  }
  
  .hero-cta-buttons {
    display: flex;
    flex-direction: row;
    gap: 16px;
    margin-top: 32px;
  }
  
  .btn-primary {
    width: auto;
    min-width: 200px;
    padding: 14px 32px;
  }
  
  .btn-secondary {
    width: auto;
    min-width: 180px;
    padding: 13px 28px;
  }
}
═══════════════════════════════════════════════════════════

SUMMARY OF ALL CHANGES:

✅ "Assistance" section: Convert to 2x2 grid (4 compact cards)
Each card ~100px height
32px icons
14px text
✅ "Tous les Types" section: Convert to horizontal cards
Icon left (44px)
Text middle (compact)
Arrow right (18px)
Each card ~72px height
Remove "Faire une demande" text
✅ Hero CTA buttons: More refined and elegant
Reduced heights (52px/48px)
Smaller font (16px)
Less rounded (6px)
Thinner borders
Subtler shadows
Side-by-side on desktop
EXPECTED RESULTS:

Assistance section: 60% height reduction
Types section: 64% height reduction
Hero buttons: More elegant, less bulky
Professional, refined appearance throughout
Faster scrolling, better mobile UX
Apply these changes immediately.


---

This prompt will:
1. ✅ Fix the bulky assistance cards → 2x2 compact grid
2. ✅ Fix the bulky service type cards → horizontal compact layout
3. ✅ Make hero buttons elegant and refined (not bulky)
4. ✅ Reduce overall page length significantly

Paste this into Replit Agent to complete the mobile optimization! 🎯